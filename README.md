# Infra Ticket Booking System (Assignment)

![This is an alt text.](https://res.cloudinary.com/djgpgveds/image/upload/v1767027686/screencapture-localhost-5173-2025-12-30-00_00_46_hvhsdv.png)

ระบบจองตั๋วออนไลน์ Full-stack (NestJS + React) พร้อมระบบแสดงผลแผนที่ GIS

## 🚀 Features (ฟีเจอร์หลัก)
- **Booking System:** ระบบจองตั๋ว, ดูรายชื่อผู้จอง, ตรวจสอบที่นั่งว่าง, ป้องกันการจองซ้ำ (Name / Email)
- **Notification:** แจ้งเตือนสถานะทันที จองเต็ม, มีที่ว่าง
- **Configurable:** ตั้งค่า Capacity ได้ผ่าน Environment Variable
- **Validation:** Form Validation (อีเมล, ชื่อ)

## 🗺️ GIS & Map Implementation
* **การจัดเก็บข้อมูล (Storage Strategy)**
    * ไม่ควรเก็บ Raw Data ไว้เองทั้งหมด ข้อมูล GIS จากหน่วยงานรัฐ (เช่น nced) มีการอัปเดตตลอดเวลาและมีขนาดใหญ่
    * ควรดึงข้อมูลจาก Source ต้นทาง (On-demand) แล้วทำ Cache ไว้ที่ Backend ของเรา (เช่นใช้ Redis หรือเก็บเป็นไฟล์ JSON ชั่วคราว) มีอายุ 24 ชม. เพื่อลดภาระการโหลดและป้องกันเว็บต้นทางล่ม
    * ถ้าต้องเก็บจริง ๆ ใช้ฐานข้อมูล PostgreSQL คู่กับ Extension PostGIS ซึ่งเก่งเรื่องการคำนวณพิกัดและการค้นหาเชิงพื้นที่
* **การดึงข้อมูลให้เร็ว (Performance Optimization)**
    * Vector Tiles (Mapbox Vector Tiles) ถ้าข้อมูลใหญ่มาก (เช่น เส้นถนนทั่วประเทศ) ควรแปลงจาก GeoJSON เป็น Vector Tiles แล้วโหลดเฉพาะส่วนที่ User กำลังดูหน้าจออยู่ (Viewport) แทนการโหลดทั้งประเทศทีเดียว
    * ไฟล์ GIS ต้นฉบับ (Shapefile) มักมีจุดพิกัดละเอียดเกินความจำเป็นสำหรับการแสดงผลบนเว็บ ทำให้โหลดช้า ใช้เครื่องมืออย่าง Mapshaper เพื่อลดจำนวนจุดพิกัด (Simplify) ลง โดยยังคงรูปร่างเดิมไว้ ช่วยลดขนาดไฟล์จาก 10MB เหลือหลัก KB ทำให้โหลดเร็วขึ้นมาก
    * *Data Processing:* แปลงข้อมูล Shapefile -> GeoJSON และ Re-project พิกัดจาก UTM เป็น WGS84 เพื่อความถูกต้องแม่นยำ

<img src="https://res.cloudinary.com/djgpgveds/image/upload/v1767032047/screencapture-localhost-5173-2025-12-30-01_12_31_qs4plx.png" alt="Booking System UI" />

## 🛠️ Tech Stack
- **Frontend:** React, TypeScript, Tailwind CSS, Leaflet (React-Leaflet)
- **Backend:** NestJS, TypeORM, PostgreSQL
- **Infrastructure:** Docker & Docker Compose

## 📦 How to Run (วิธีรันโปรเจกต์)

### แบบที่ 1: รันด้วย Docker (แนะนำ 🌟)
รันคำสั่งเดียว ระบบจะสร้าง Database และเชื่อมต่อให้เองอัตโนมัติ:
```
cd infra-assignment
docker-compose up -d 
```

**Frontend**: http://localhost:5173

**Backend API**: http://localhost:3000

**Database Admin (pgAdmin4)**: http://localhost:8080 (Email: admin@admin.com / Pass: password123)

### แบบที่ 2: รันแยก Manual
**Database**:
```
docker-compose up db -d
```
**Backend**:
```
cd backend
npm install
npm run start
```
**Frontend**:
```
cd frontend
npm install
npm run dev
```
## ⚙️ Environment Setup (การตั้งค่า)

สร้างไฟล์ `.env` ไว้ที่ root directory ของโปรเจกต์และกำหนดค่าดังนี้:

| Variable | Description | Default |
| :--- | :--- | :--- |
| `DB_HOST` | Hostname ของฐานข้อมูล (PostgreSQL) | localhost |
| `DB_PORT` | Port ของฐานข้อมูล | 5432 |
| `DB_USER` | Username สำหรับเข้าใช้งาน DB | admin |
| `DB_PASS` | Password สำหรับเข้าใช้งาน DB | password123 |
| `DB_NAME` | ชื่อฐานข้อมูล | booking_system |
| **`BOOKING_CAPACITY`** | **จำนวนที่นั่งสูงสุดที่เปิดให้จอง (เปลี่ยนได้ตรงนี้)** | **10** |

Example:
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASS=password123
DB_NAME=booking_system

# Booking Configuration
BOOKING_CAPACITY=10  # กำหนดจำนวนที่นั่งสูงสุดที่เปิดรับจอง
```

### 🧪 Testing (ตัวอย่างการทดสอบ API)

สามารถทดสอบด้วย cURL หรือ Postman ดังนี้:

**1. จองตั๋ว (Create Booking)** Windows CMD / PowerShell
```
curl -X POST http://localhost:3000/bookings -H "Content-Type: application/json" -d "{\"name\":\"Somchai\",\"email\":\"somchai@test.com\"}"
```
**2. ดูรายการจองทั้งหมด (Get All)**
```
curl http://localhost:3000/bookings
```
**3. ยกเลิกการจอง (Delete)**
```
curl -X DELETE http://localhost:3000/bookings/somchai@test.com
```
